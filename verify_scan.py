import pandas as pd
from datetime import datetime
import os
from market_app import run_velocity_mode # I need to rename market-app.py to market_app.py or import differently

# Since market-app.py has a '-' in the name, I'll just copy the logic or rename it temporarily
# Better: Just run a small part of it.
